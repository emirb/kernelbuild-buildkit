# Categories for GitHub's generated release notes (release.yml creates the
# release with --generate-notes). Dependabot PRs get their own section.
changelog:
  categories:
    - title: Changes
      labels: ["*"]
      exclude:
        labels: [dependencies]
    - title: Dependencies
      labels: [dependencies]
